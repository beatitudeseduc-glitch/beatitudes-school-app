import { boolean, date, numeric, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
export const portalRole = pgEnum("portal_role", ["management", "teacher"]);
export const attendanceStatus = pgEnum("attendance_status", ["Present", "Absent", "Late"]);
export const profiles = pgTable("profiles", { id: text("id").primaryKey(), email: text("email").notNull().unique(), fullName: text("full_name").notNull(), role: portalRole("role").notNull(), phone: text("phone"), active: boolean("active").notNull().default(true), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() });
export const classes = pgTable("classes", { id: uuid("id").primaryKey().defaultRandom(), name: text("name").notNull().unique() });
export const teacherClasses = pgTable("teacher_classes", { teacherId: text("teacher_id").notNull().references(() => profiles.id, { onDelete: "cascade" }), classId: uuid("class_id").notNull().references(() => classes.id, { onDelete: "cascade" }) }, (t) => [uniqueIndex("teacher_class_unique").on(t.teacherId, t.classId)]);
export const parents = pgTable("parents", { id: uuid("id").primaryKey().defaultRandom(), fullName: text("full_name").notNull(), phone: text("phone").notNull(), email: text("email"), address: text("address"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() });
export const pupils = pgTable("pupils", { id: uuid("id").primaryKey().defaultRandom(), admissionNumber: text("admission_number").notNull().unique(), fullName: text("full_name").notNull(), gender: text("gender"), dateOfBirth: date("date_of_birth"), classId: uuid("class_id").notNull().references(() => classes.id), parentId: uuid("parent_id").references(() => parents.id, { onDelete: "set null" }), active: boolean("active").notNull().default(true), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() });
export const attendance = pgTable("attendance", { id: uuid("id").primaryKey().defaultRandom(), pupilId: uuid("pupil_id").notNull().references(() => pupils.id, { onDelete: "cascade" }), attendanceDate: date("attendance_date").notNull(), status: attendanceStatus("status").notNull(), recordedBy: text("recorded_by").notNull().references(() => profiles.id), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => [uniqueIndex("attendance_pupil_date_unique").on(t.pupilId, t.attendanceDate)]);
export const feeRecords = pgTable("fee_records", { id: uuid("id").primaryKey().defaultRandom(), pupilId: uuid("pupil_id").notNull().references(() => pupils.id, { onDelete: "cascade" }), term: text("term").notNull(), item: text("item").notNull(), amountDue: numeric("amount_due", { precision: 12, scale: 2 }).notNull(), amountPaid: numeric("amount_paid", { precision: 12, scale: 2 }).notNull().default("0"), paymentDate: date("payment_date"), reference: text("reference"), recordedBy: text("recorded_by").notNull().references(() => profiles.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() });
export const results = pgTable("academic_results", { id: uuid("id").primaryKey().defaultRandom(), pupilId: uuid("pupil_id").notNull().references(() => pupils.id, { onDelete: "cascade" }), subject: text("subject").notNull(), assessment: text("assessment").notNull(), term: text("term").notNull(), score: numeric("score", { precision: 6, scale: 2 }).notNull(), maximumScore: numeric("maximum_score", { precision: 6, scale: 2 }).notNull().default("100"), recordedBy: text("recorded_by").notNull().references(() => profiles.id), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => [uniqueIndex("result_unique").on(t.pupilId, t.subject, t.assessment, t.term)]);
export const notices = pgTable("notices", { id: uuid("id").primaryKey().defaultRandom(), title: text("title").notNull(), body: text("body").notNull(), audience: text("audience").notNull().default("all"), createdBy: text("created_by").notNull().references(() => profiles.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() });
